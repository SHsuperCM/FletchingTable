plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    // Define the plugin
    plugins {
        fletchingtable {
            id = 'shcm.shsupercm.fabric.fletchingtable'
            implementationClass = 'shcm.shsupercm.fabric.fletchingtable.FTGradlePlugin'
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation group: 'com.google.code.gson', name: 'gson', version: '2.8.9'
}